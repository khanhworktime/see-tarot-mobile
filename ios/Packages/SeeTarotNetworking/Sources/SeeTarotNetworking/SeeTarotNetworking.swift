import Foundation
import SeeTarotCore

/// SeeTarotNetworking namespace. See `APIClientProtocol`, `LiveAPIClient`,
/// `StubAPIClient`, `SSEParser`. Depends only on SeeTarotCore (decision 0004).
public enum SeeTarotNetworking {
    public static let moduleName = "SeeTarotNetworking"
}
