import Foundation

/// SeeTarotFeatures namespace. Entry point: `AppComposition.makeAuthStore` →
/// `RootView`. Composes all layers (decision 0004).
public enum SeeTarotFeatures {
    public static let moduleName = "SeeTarotFeatures"
}
