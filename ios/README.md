# ios/

Native iOS app (Swift). Phase 1 focus.

Architecture: Approach A — SwiftUI + `@Observable` MVVM + local Swift Package
modules + Core Animation/UIKit for cards + Metal ambient background. See
`docs/decisions/0004-native-ios-app-architecture.md` and
`docs/stories/epics/E01-ios-foundation/`.

No source scaffolded yet (Harness v0). Xcode workspace + SPM packages are
created when E01 implementation starts.
